# Database — use Neon or Vercel Postgres in production
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/luxlane?schema=public"

# Auth — 32+ char random string
AUTH_SECRET="change-me-to-a-32-plus-char-random-secret"

# App URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Vercel Blob (file uploads) — Sprint 1+
BLOB_READ_WRITE_TOKEN=""

# Upstash Redis (rate limit + realtime) — Sprint 5+
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# Resend (transactional email) — Sprint 7
RESEND_API_KEY=""
EMAIL_FROM="LuxLane <no-reply@luxlane.app>"
